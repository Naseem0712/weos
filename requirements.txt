# WEOS — Window Engineering Operating System
fastapi>=0.110,<1
uvicorn[standard]>=0.27,<1
pydantic>=2.0,<3
ezdxf>=1.0,<2
reportlab>=4.0,<5
svglib>=1.5,<2
Pillow>=10.0,<13
qrcode[pil]>=7.4,<9
openpyxl>=3.1,<4
python-multipart>=0.0.9,<1
lxml>=5.0,<6
pypdf>=4.0,<6
# Cloud-first persistence (Part 1 / Part 9). Production uses PostgreSQL via
# DATABASE_URL (Railway); local dev degrades to sqlite through the same layer.
SQLAlchemy>=2.0,<3
psycopg2-binary>=2.9,<3
# Optional (tables / page renders) — Learning Engine degrades gracefully if missing:
# pdfplumber>=0.11
# pypdfium2>=4.0
